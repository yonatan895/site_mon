# HMC Platform - Common Fields
# Shared configuration inherited by all HMC data type rules.
platform: hmc

common_fields:
  platform: hmc

metric_prefix: "hmc"
default_sourcetype: "hmc:metrics"
default_index: "mainframe_metrics"
interval_seconds: 300
