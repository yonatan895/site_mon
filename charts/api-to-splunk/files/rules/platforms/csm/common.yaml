# CSM Platform - Common Fields
# Shared configuration inherited by all CSM data type rules.
platform: csm

common_fields:
  platform: csm

metric_prefix: "csm"
default_sourcetype: "csm:metrics"
default_index: "mainframe_metrics"
interval_seconds: 300
