apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-to-splunk-ds
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://git.internal.example.com/monitoring/api-to-splunk.git
    targetRevision: main
    path: charts/api-to-splunk
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
      values: |
        global:
          platform: ds
        vault:
          secretName: "api-to-splunk-ds-secrets"
        networkPolicy:
          apiEndpoints:
            - ds-main.example.com
            - ds-dr.example.com
  destination:
    server: https://kubernetes.default.svc
    namespace: mainframe-monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
